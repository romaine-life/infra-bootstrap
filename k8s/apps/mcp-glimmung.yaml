apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-glimmung
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/romaine-life/mcp-glimmung.git
    targetRevision: main
    path: chart
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-glimmung
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
