apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: ng6-external-dns
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: https://ng6-external-dns.vault.azure.net/
      serviceAccountRef:
        name: external-secrets
        namespace: external-secrets
