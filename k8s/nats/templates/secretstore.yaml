apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: ng6-nats
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: https://ng6-nats.vault.azure.net/
      serviceAccountRef:
        name: external-secrets
        namespace: external-secrets
